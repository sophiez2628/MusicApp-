<h1>Add a Band</h1>
<form action="/bands" method="POST">
  <%= auth_token %>
  <label for="band_name">Name</label>
  <input type="text"
         name="band[name]"
         value="<%= @band.name %>"
         id="band_name">
  <input type="submit" value="create band">
</form>
